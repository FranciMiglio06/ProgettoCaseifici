export interface Forma {
  for_id: string;
  for_data: string;
  for_num_forma: number;
  for_nome: string;
  for_peso: number;
  for_scelta: string;
  for_stag_eff: number;
  for_venduta: number;
  for_data_acquisto: string | null;
  for_dat_id: number;
  for_prezzo_reale: number;
  for_tip_id: number;
  for_sta_id: string;  }
